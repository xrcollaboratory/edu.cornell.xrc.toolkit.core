using UnityEngine;

namespace XRC.Toolkit.Core.Samples.ARFoundationUtilities
{
    public class MarkersSetup : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
